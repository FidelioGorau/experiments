<?php
$title = 'My resolved bugs';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<?php echo $this->navigation('navigation')->menu()->render();?>
<table class="table">
    <tr>
        <th>Title</th>
        <th>Created</th>
        <th></th>
    </tr>
    <?php
    foreach($this->posts as $post) {
        print '<tr><td>
        <a href="' . $this->url('bugtracker', array('action'=>'view', 'id' => $post->getId())) . '">' . $post->getTitle() . '</a>
        <td>' . date('d.m.Y H:i', $post->getCreated()). '</td>
        <td><div class="btn-sm-group" role="group">
        <a class="btn-sm btn-info" href="' . $this->url('bugtracker', array('action'=>'edit', 'id' => $post->getId())) . '">edit</a>
        <a class="btn-sm btn-danger" href="' . $this->url('bugtracker', array('action'=>'delete', 'id' => $post->getId())) . '">delete</a>
        </div></td>
        </tr>';
    }
    ?>
</table>