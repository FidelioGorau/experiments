<?php
$title = 'List of active bugs';
$this->headTitle($title);
?>
<?php if(!$this->zfcUserIdentity()): ?>
    <div class="alert alert-danger" role="alert">Registration danger!!!</div>
<?php endif;?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<table class="table">
    <tr>
        <th>Title</th>
        <th>Created</th>
    </tr>
    <?php
    foreach($this->posts as $post) {

        print '<tr><td>
            <a href="' . $this->url('bugtracker', array('action'=>'view', 'id' => $post->getId())) . '">' . $post->getTitle() . '</a></td>
            <td>' . date('d.m.Y H:i', $post->getCreated()). '</td>
        </tr>';
    }
    ?>
</table>